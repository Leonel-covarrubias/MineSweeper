version: 2.1

jobs:
  test:
    docker:
      - image: circleci/python:3.8.3
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            pip install pytest coverage
      - run:
          name: Run unit tests
          command: pytest testFIle.py
      - run:
          name: Generate coverage report
          command: coverage run --source=Minesweeper.py -m pytest testFile.py && coverage report

workflows:
  build-and-test:
    jobs:
      - test